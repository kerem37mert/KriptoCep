<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Buton Stili -->
    <style name="goBtn" parent="Widget.AppCompat.Button">
        <item name="android:background">@drawable/rounded_corner_btn</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:padding">10dp</item>
    </style>

    <!-- EditText Stili -->
    <style name="editText" parent="Widget.AppCompat.EditText">
        <item name="android:background">@drawable/rounded_corner_edittext</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:padding">10dp</item>
    </style>

    <!-- Standart TextView Stili -->
    <style name="stdText" parent="Widget.AppCompat.TextView">
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">14sp</item>
    </style>

    <style name="bottomBar" parent="Widget.AppCompat.ActionBar">
        <item name="android:background">@drawable/rounded_corner_navigation</item>
        <item name="itemIconTint">@color/white</item>
        <item name="itemTextColor">#120D0D</item>
    </style>

    <style name="coinListItem">
        <item name="cardCornerRadius">5dp</item>
        <item name="cardElevation">4dp</item>
    </style>

    <style name="transactionBtn">
        <item name="android:background">@drawable/rounded_corner_btn</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">14sp</item>
    </style>

    <style name="changeBtn">
        <item name="android:background">@drawable/rounded_change_btn</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">12sp</item>
    </style>

    <style name="addTransactionBtn">
        <item name="android:background">@drawable/rounded_corner_btn</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">14sp</item>
    </style>

    <style name="updateBtn">
        <item name="android:background">@drawable/rounded_corner_btn</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">14sp</item>
        <item name="android:paddingLeft">40dp</item>
        <item name="android:paddingRight">40dp</item>
    </style>
</resources>